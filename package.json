{
  "name": "omnirent",
  "version": "1.0.0",
  "description": "Unified runner for OmniRent marketplace",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm run dev",
    "dev": "npx -y concurrently \"npm run backend\" \"npm run frontend\" --kill-others --names \"BACKEND,FRONTEND\" --prefix-colors \"yellow,blue\""
  }
}
